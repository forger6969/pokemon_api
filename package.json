{
  "name": "my-json-server",
  "version": "1.0.0",
  "description": "Простой проект с json-server для имитации REST API",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch db.json --port 10000",
    "dev": "json-server --watch db.json --port 10000 --delay 500",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "json-server",
    "mock-api",
    "rest-api"
  ],
  "author": "Saidazim",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}